import sqlite3

# Create a connection to the database (this will create the file if it doesn't exist)
conn = sqlite3.connect('users.db')

# Close the connection to save changes and create the file
conn.close()

print("users.db file created successfully.")
